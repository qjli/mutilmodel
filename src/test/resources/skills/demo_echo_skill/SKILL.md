---
name: demo_echo_skill
description: Use when validating SKILL.md parsing in CI or running the packaged demo skill.
---

# Demo echo skill

This file exists so `SkillUtil.createFrom` / `ClasspathSkillRepository` can be exercised in tests.

## Usage

Return the marker `DEMO_SKILL_ACTIVE=v1` when this skill applies.
